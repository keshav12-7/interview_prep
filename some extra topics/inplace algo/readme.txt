An in-place sorting algorithm sorts the array using only a constant amount of additional memory, typically O(1), by modifying the original array instead of creating a new one.

An in-place sorting algorithm sorts the array without using additional memory proportional to input size, typically requiring only 
𝑂
(
1
)
O(1) extra space.

Common in-place sorting algorithms:

Selection Sort

Bubble Sort

Insertion Sort

Heap Sort

Quick Sort (mostly in-place)